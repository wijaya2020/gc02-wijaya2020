export default function NavBar({ setPage }) {
  return (
    <>
      {/* <nav className="sticky top-0 z-100 mx-4 p-3 bg-purple-400 border-2 border-black rounded-2xl shadow-[2px_2px_0px_rgba(0,0,0,1)] flex justify-center ">
        <div>
          <a
            className="text-2xl text-white font-bold px-6 hover:text-black cursor-pointer"
            onClick={() => setPage("home")}
          >
            <span>Home</span>
          </a>

          <a
            className="text-2xl text-white font-bold px-5 hover:text-black cursor-pointer"
            onClick={() => setPage("detailProduct")}
          >
            <span>Product Detail</span>
          </a>

          <a
            className="text-2xl text-white font-bold px-5 hover:text-black cursor-pointer"
            onClick={() => setPage("login")}
          >
            <span>Logout</span>
          </a>
        </div>
      </nav> */}
    </>
  );
}
